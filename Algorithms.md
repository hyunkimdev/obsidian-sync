
```python
print("문장1 문장2")  # O(1) 시간복잡도, O(1) 공간복잡도
print("문장1", "문장2")  # O(1) 시간복잡도, O(1) 공간복잡도
print("문장1" + " " + "문장2")  # O(n) 시간복잡도, O(n) 공간복잡도
'''
1. 두 문자열을 합치는 작업 자체가 추가 작업이기 때문에 시간복잡도가 O(n)으로 증가한다.
2. 새로운 문자열이 생기기 때문에 메모리가 더 필요하므로 공간복잡도가 O(n)으로 증가한다.
'''
```